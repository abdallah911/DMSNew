@model DMS_Authontication1.ViewModel.EmployeeDataViewModel

@{
    ViewBag.Title = "Search";
    Layout = "~/Views/Shared/_Layout_DashboardHomePage.cshtml";

}
@*<h2>Index</h2>*@
<script src="~/assets/HomeTemplete/scripts/jquery-3.0.0.js"></script>
<script src="~/assets/HomeTemplete/Scripts/toastr.min.js"></script>
<script src="~/assets/HomeTemplete/scripts/select2.min.js"></script>
@*<input type="text" id="compname" value="@ViewBag.CompName" hidden="hidden" />*@
<style>
    .form-control {
        background: transparent;
    }

    .form-horizontal .form-group {
        display: flex
    }

        .form-horizontal .form-group label {
            width: 20%;
        }

        .form-horizontal .form-group div {
            width: 80%;
        }

    select#ddlDiagnoises,
    input.admin-form-control {
        border-radius: 6px;
    }
</style>
<div style="min-height: calc(100vh - 275.9px); direction: rtl; " class="ar-page pb-4">

    <div hidden>
        <input id="Speciality" name="Speciality" value="" />
        <input id="DiagnoisesJson" name="DiagnoisesJson" value="" />
    </div>
    <div class="panel panel-info" style="direction:ltr">
        <h2 class="panel-heading title-bg text-white fw-bold" style="margin:1px;">Create Claim</h2>
        <div class="panel-body ">
            @if (ViewBag.error != null)
            {
                <h4 style="color:red;"> Claim added befor for this card</h4>
            }
            <div class="form-horizontal col-md-6 mx-auto row row-gap-2" style="padding-left: 93px;">
                @Html.ValidationSummary("", new { @class = "text-danger" })
                <div class="form-group">
                    <label>Card ID</label>
                    <div class="admin-form-control">
                        @Html.TextBoxFor(m => m.CardId, new { @class = "form-control", @required = "true" })
                    </div>
                </div>
                <div class="form-group">
                    <label>Claim Number</label>
                    <div class="admin-form-control">
                        <textarea class="form-control" id="ClaimNumber" required></textarea>
                    </div>
                </div>

                <div class="form-group">
                    <label>Speciality</label>
                    <div class="">
                        <select class="form-control" id="ddlSpeciality" required></select>
                    </div>
                </div>
                <div class="form-group">
                    <label>Diagnoise</label>
                    <div class="">
                        <select id="ddlDiagnoises" style="width:100%" multiple required></select>
                    </div>
                </div>

                <div class="form-group">
                    <label>Claim Photo</label>
                    <div class="">
                        @*<input type="file" id="wizard-picture" name="MainFileFile" accept="application/pdf" />*@
                        <input class="form-control admin-form-control p-2 text-box single-line" id="ImageFile" name="ImageFile" required accept="image/*" type="File" value="">
                    </div>
                </div>
            </div>
        </div>
        <div class="panel-footer col-md-6 mx-auto mt-3 text-center text-sm-end pe-sm-2" style="text-align:right;">
            <input id="Back" type="button" value="Back" onclick="window.location.replace('/Claim/index');" class="btn btn-warning" />
            @*<input id="New" type="reset" value="Reset" class="btn btn-primary" />*@
            <input id="submit" type="submit" value="Save" onclick="Submitform();" class="btn btn-success" />
        </div>
    </div>
</div>
<script>
    $(function () {
        $("#ddlSpeciality").change(function () {
            $("#wait").css("display", "block");
            $.ajax({
                type: 'POST',
                url: '/Pharmacy/getDiag/',
                dataType: 'json',
                data: { id: $("#ddlSpeciality").val() },
                success: function (r) {
                    $('#ddlDiagnoises').empty();
                    var result = [];
                    for (var i = 0; i < r.length; i++) {
                        var current = {};
                        current.id = r[i].Code;
                        current.text = r[i].Name;
                        result.push(current);
                    }
                    $('#ddlDiagnoises').select2({
                        data: result
                    })
                    $("#wait").css("display", "none");

                },
                error: function (ex) {
                    bootbox.alert('Failed to retrieve Diagnoses , please check your internet connection');
                    $("#wait").css("display", "none");

                }

            });
        });
        $("#ddlSpeciality").map(function () {
            $.ajax({
                type: 'POST',
                url: '/Pharmacy/getDiag/',
                dataType: 'json',
                data: { id: $("#ddlSpeciality").val() },

                success: function (r) {
                    $('#ddlDiagnoises').empty();
                    var result = [];
                    for (var i = 0; i < r.length; i++) {
                        var current = {};
                        current.id = r[i].Code;
                        current.text = r[i].Name;
                        result.push(current);
                    }
                    $('#ddlDiagnoises').select2({
                        data: result
                    })

                    $("#wait").css("display", "none");

                },
                error: function (ex) {
                    bootbox.alert('Failed to retrieve Diagnoses, please check your internet connection');
                }

            });
        });
        $("#ddlSpeciality").select2();
        //$('#submit').click(function () {
        //    var SelectedDiagnosisList = $('#ddlDiagnoises').select2('data');
        //    var diagnose = [];
        //    for (var i = 0; i < SelectedDiagnosisList.length; i++) {
        //        var current = {};
        //        //current.DIAG_CODE = SelectedDiagnosisList[i].id;
        //        current.DIAG_ANAME = SelectedDiagnosisList[i].text;
        //        diagnose.push(current);
        //    }
        //    $("#Speciality").val($('#ddlSpeciality option:selected').text());
        //    $("#ClaimPhotoDiagnoises").val(diagnose);
        //    $("#createForm").submit();
        //});
    });
    function Submitform() {
        var SelectedDiagnosisList = $('#ddlDiagnoises').select2('data');
        var diagnose = [];
        for (var i = 0; i < SelectedDiagnosisList.length; i++) {
            //var current = {};
            //current.DIAG_CODE = SelectedDiagnosisList[i].id;
            //current.DIAG_ANAME = SelectedDiagnosisList[i].text;
            diagnose.push(SelectedDiagnosisList[i].text);
        }
        $("#Speciality").val($('#ddlSpeciality option:selected').text());
        //$("#SpecialOpportunityUsersJson").val(JSON.stringify(specialopport));

        $("#DiagnoisesJson").val(JSON.stringify(diagnose));
        $("#createForm").submit();
    }
</script> 