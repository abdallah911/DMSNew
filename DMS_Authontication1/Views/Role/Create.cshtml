@model DMS_Authontication1.Models.RoleViewModel

@{
    ViewBag.Title = "Create";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="panel panel-info dms-container">
        <h2 class="panel-heading" style="margin:1px;">Create Role</h2>
        <div class="panel-body">
            <div class="form-horizontal">
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                <div class="form-group">
                    @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                    </div>
                </div>

                @*<div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Create" class="btn btn-default" />
                    </div>
                </div>
            </div>*@
        </div></div>
            
        <div class="panel-footer" style="text-align:right;">
            <input id="Back" type="button" value="Back" onclick="window.location.replace('/Role/index');" class="btn btn-warning" />
            <input id="submit" type="submit" value="Create" class="btn btn-success" />
        </div>
    </div>
    <br />
}
